<p id="notice"><%= notice %></p>

<h2>You are currently standing at...</h2>
<p><%= @location.my_location %></p>

<h2>Buses closest to you are...</h2>
<% if @bus_count == 0 %>
 <p>...not really that close.  Time to walk, bike, taxi, or Uber.</p>
<% else %>
 <% @nearby_buses.each do |bus| %>
   <ul>
     <li><strong>Bus #<%= bus["VEHICLE"] %></strong> (<%= bus["ROUTE"] %> Route): 
     Heading <%= bus["DIRECTION"] %> towards the <%= bus["TIMEPOINT"] %> stop.</li>
   </ul>
 <% end %>
<% end %>

<%= link_to 'New Search', edit_location_path(@location) %>